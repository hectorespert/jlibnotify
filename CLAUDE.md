# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The full guidance for this repository lives in [AGENTS.md](AGENTS.md), shared by all AI coding
agents. Read it before making changes — it covers the build and test commands (including how to run a
single test and why the `*IT` integration tests need a real `libnotify.so.4`), the four-layer
architecture from public API down to the JNA binding, the `GBoolean` and `ServerCapabilitiesReader`
conventions, the mock-based testing strategy, and the tag-driven release process.
